Envío de códigos ID:8uf98us93

TEMA: Publicación de página con posible base de datos en render.com

1.- Cuenta en gihub.com

2.- Instalar en computadora nodejs y npm, luego instalar n para establecer la última versión de node

4.- Crear un directorio para el proyecto y entrar al directorio

5.- Isntrucción en la terminal: npm i express dotenv ejs

6.- Hacer el archivo index.js:

    //CÓDIGO MÍNIMO PARA POSIBLES POST, PLANTILLAS EJS Y ARCHIVO DE CONTANTES DOTENV:

    const express = require('express')
    const ejs = require('ejs')
    const path = require('path')
    const bodyParser = require('body-parser')
    const port = process.env.PORT || 8000
    const app = express()
    require('dotenv').config()

    //settings
    app.use(express.static(__dirname + "/public"));  //PARA CSS
    app.use(bodyParser.urlencoded({extended:false})) //NECESARIOOOO
    app.use(bodyParser.json());    

    app.set('views',path.join(__dirname, 'views'))
    app.set('view engine','ejs')

    //routes
    app.get('/',(req,res)=>res.render('main',{title:'Título'}))

    //starting server
    app.listen(port, () => {  
        console.log(`Servidor en la url http://127.0.0.1:${port}/`)
    })

7.- Crear un directorio views y hacer el archivo main.ejs con el siguiente contenido:

    <!DOCTYPE html>
    <html lang="en">
      <head>
        <meta charset="utf-8">
        <title>title</title>
        <link rel="stylesheet" href="main.css">
        <!--script src="script.js"></script-->
      </head>
      <body>
        <H2><%=title %></H2>
        <!-- page content -->
      </body>
    </html>

7.- Ejecutar con: node index
.- detener con ctrl+c
8.- Hacer ctrl + click en http://127.0.0.1:8000/ o escribirlo en la barra de url del navegador

.- Ir a github y crear un nuevo repositorio
.- En local si no está instalado git, instalar

en otra terminal:
git config --global user.email "you@example.com"
  git config --global user.name "Your Name"

https://docs.github.com/es/authentication/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent
ssh-keygen -t ed25519 -C "your_email@example.com"

https://docs.github.com/es/authentication/connecting-to-github-with-ssh/adding-a-new-ssh-key-to-your-github-account





Importante hacer el archivo .gitignore para que no se suba todo el node modules
Colocar en el arcivo:
node_modules/





git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:xxxxxtuusuario/xxxxxtuproyecto.git
git push -u origin main


Ir a render-com -> dashboard -> add new -> web service -> Si pide conectar al servicio de git, agregar ese servicio, si ya está agregado, connect repository donde dice public git repositori colocar la dirección url de tu repositorio
ej https://github.com/xxxxxtuusuario/xxxxxtuproyecto.git

colocar los datos que piden como el
Nombre del proyecto
Branch: main
Runtime node
Build command npm install
Start command node index
